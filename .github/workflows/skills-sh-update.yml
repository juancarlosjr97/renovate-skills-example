name: Skills.sh Update

on:
  schedule:
    - cron: "*/5 * * * *"
  push:
    branches:
      - main
  
permissions:
  contents: write
  pull-requests: write

jobs:
  skills-sh-update:
    uses: juancarlosjr97/github-actions-workflows-to-rule-them-all/.github/workflows/shared-agents-skills-sh-update.yml@main
